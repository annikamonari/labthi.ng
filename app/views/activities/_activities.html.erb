<% activities.each do |activity| %>
  <div class="activity">
    <%= link_to (activity.owner.name || activity.owner.email), activity.owner.profile if activity.owner %>
    <%= render_activity activity %>
  </div>
<% end %>