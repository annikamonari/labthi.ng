<%= form_for(current_user.idea_relationships.find_by(followed_id: @idea.id), 
    remote: true,
    html: {method: :delete}) do |f| %>
  <%= f.submit "Unfollow",
    class: "btn btn-success pull-right unfollow-idea-button" %>
<% end %>
