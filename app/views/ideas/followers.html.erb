<p id="notice"><%= notice %></p>

<%= render 'secondary_nav' %>
<%= render 'sidebar' %>

<hr>
<h4><%= pluralize(@idea.followers.count, "follower") %> of <b><%=@idea.title %></b></h4>

<div class="content-bg">
  <div class="idea-followers">
    <% @idea_followers.each do |user, points| %>
        <%= render 'users/team_member', user: user, points: points %>
    <% end %>
  </div>
</div>