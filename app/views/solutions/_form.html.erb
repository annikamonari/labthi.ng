<%= form_for(@solution) do |f| %>
  <% if @solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% @solution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brief %><br>
    <%= f.text_area :brief %>

    <% if user_signed_in? %>
    <%= hidden_field_tag :aspect_id, params[:aspect_id] %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Solution', :class => "btn btn-primary" %>
  </div>
<% end %>
