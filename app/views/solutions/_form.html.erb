<%= form_for(@solution, remote:true) do |f| %>
  <% if @solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% @solution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brief %><br>
    <%= f.text_area :brief, class: "form-control" %><br>
  </div>
  <% if is_image_submission? %>
    <div class="field form-group">
      <%= f.label :image %><br>
      <%= f.file_field :image %>
    </div>
  <% end %>

    <%= hidden_field_tag :aspect_id, params[:aspect_id] %>
    <%= hidden_field_tag :idea_id, params[:idea_id] %>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
