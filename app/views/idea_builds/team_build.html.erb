<%= render 'idea_builds/secondary_nav' %>
<div class='col-md-8 no-gutter'>
  <%= render 'idea_builds/brief' %>
  <%= render 'idea_builds/secondary_sub_nav', locals: { url: 'btn btn-default text ' + active_when(idea_build_team_build_path) } %>
  <br>
  <div class='row no-gutter'>
    <div class='welcome-block clearfix'>
      <h4 class='pull-left'>Team Build</h4>
      <button href='javascript:void(0);' class='btn btn-info pull-right' style='width:114px;margin-right:13px;padding-left:9px;' align='center' id='newpost'>Create Proposal</button>
    </div>
    <div class='content-bg' style='display:none;' id='posts'>
      <%= render partial: 'posts/form', locals: { url: create_proposal_path, placeholder: 'Write a proposal to gain team members for your start-up. Explain what you hope to achieve with your team members, the skills you have to offer, and the skills you hope they can contribute to the start-up. You should also tell the community a bit about yourself, how you like to work, and in how long you hope to finish building the start-up.', title: 'New Proposal' } %><br>
      <%= link_to 'Cancel', 'javascript:void(0);', id: 'cancel' %>
    </div>
    <ul class="posts media-list">
      <% if @posts.count > 0 %>
        <%= render (@posts.sort_by {|p| p.created_at}).reverse %>
      <% else %>
        <div class='content-bg no posts' id='noposts'>
          There are no proposals yet. Why don't you add one to get your team started?
        </div>
      <% end %>
    </ul>
    <br><br><hr>
  </div>
</div>
<div class='col-md-4'>
  <%= render 'idea_builds/sidebar' %>
</div>