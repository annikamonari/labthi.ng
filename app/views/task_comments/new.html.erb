<h1>New task_comment</h1>

<%= form_for @task_comment, url: create_task_comment_path do |f| %>
  <% if @task_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_comment.errors.count, "error") %> prohibited this task_comment from being saved:</h2>

      <ul>
      <% @task_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', edit_part_path %>
