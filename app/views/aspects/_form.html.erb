<%= form_for(@aspect) do |f| %>
  <% if @aspect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aspect.errors.count, "error") %> prohibited this aspect from being saved:</h2>

      <ul>
      <% @aspect.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
    <%= f.label :brief %><br>
    <%= f.text_area :brief %>
    
  </div>
  <div class="actions">
    <%= f.submit 'Save Aspect', :class => "btn btn-primary" %>
  </div>

<% end %>
