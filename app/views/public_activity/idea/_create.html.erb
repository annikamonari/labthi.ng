created an idea

<% if activity.trackable %>
  <%= link_to activity.trackable.title, activity.trackable %> <%= time_ago_in_words(activity.created_at) %> ago
<% else %>
  which has since been removed
<% end %>
<br />