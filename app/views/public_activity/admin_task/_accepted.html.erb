accepted an admin task

<% if activity.trackable %>
  <%= link_to activity.trackable.title, edit_part_path(id: activity.trackable.part.id, idea_id: activity.trackable.part.idea.id) %>
<% else %>
  which has since been removed
<% end %>
<br />