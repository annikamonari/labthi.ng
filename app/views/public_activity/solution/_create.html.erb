added a solution

<% if activity.trackable %>
  <%= link_to truncate(activity.trackable.brief, length: 70), activity.trackable %> <%= time_ago_in_words(activity.created_at) %> ago
<% else %>
  which has since been removed
<% end %>
<br />