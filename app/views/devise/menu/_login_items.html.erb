<% if user_signed_in? %>
    <%= nav_link(current_user.name, current_user.profile, "show-user-profile")  %>
    <li>
      <%= link_to('Sign out', destroy_user_session_path, :method => :delete) %>
    </li>
<% else %>
      <%= nav_link('Log in', '/login')  %>
<% end %>